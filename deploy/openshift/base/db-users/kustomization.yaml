# Sin pvc.yaml: el StatefulSet usa volumeClaimTemplates (1 PVC de 1Gi,
# ReadWriteOnce, sin storageClassName fijo) en vez de un PVC independiente.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - statefulset.yaml
  - service.yaml
