apiVersion: v1
kind: Service
metadata:
  name: db-events
  labels:
    app.kubernetes.io/name: event-hub
    app.kubernetes.io/component: db-events
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: event-hub
    app.kubernetes.io/component: db-events
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
